blyadskiy counter = 5;

ebal main() {
        blyadskiy a = 1;
        blyadskiy b = 1;
        pokazhi(a);
        pokazhi(b);
        dohuya(counter) {
                blyadskiy tmp = b;
                b = a + b;
                a = tmp;
                pokazhi(b);
                counter = counter - 1;
        }
        otday 0;
}

