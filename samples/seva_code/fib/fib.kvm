var counter = 5;

func main() {
        var a = 1;
        var b = 1;
        output(a);
        output(b);
        while(counter) {
                var tmp = b;
                b = a + b;
                a = tmp;
                output(b);
                counter = counter - 1;
        }
        return 0;
}
